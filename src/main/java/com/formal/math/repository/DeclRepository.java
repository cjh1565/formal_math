package com.formal.math.repository;

import com.formal.math.entity.Decl;
import org.springframework.data.jpa.repository.JpaRepository;

public interface DeclRepository extends JpaRepository<Decl, Long> {
}
